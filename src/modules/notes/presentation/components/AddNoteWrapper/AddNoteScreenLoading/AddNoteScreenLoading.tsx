import { ScreenLoading } from '~components/Loading';

export const AddNoteScreenLoading = () => {
  return (
    <ScreenLoading className="fixed left-0 top-0 z-10 bg-slate-300 bg-opacity-30" />
  );
};
